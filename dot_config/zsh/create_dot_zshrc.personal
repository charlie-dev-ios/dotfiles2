# 私用の端末でのみ書く zsh 設定 (.zshrc から自動で読み込まれる)
#
# このファイルは chezmoi の create_ 属性で管理されている。
#   - 端末上に無ければ chezmoi apply で作成される
#   - 既に存在する場合は中身を一切上書きしない
#   - 中身はリポジトリに同期されない (各端末でローカルに編集する)
# 個人用のエイリアスや環境変数などをここに書く。
#
# 例:
#   alias g='git'
#   export EDITOR='nvim'
